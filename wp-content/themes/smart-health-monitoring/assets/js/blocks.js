/**
 * Gutenberg Block Editor customization
 */

(function(wp) {
	'use strict';

	if (!wp || !wp.blocks) {
		return;
	}

	// Custom block styles can be registered here
	// Example:
	// wp.blocks.registerBlockStyle('core/button', {
	//   name: 'shm-primary',
	//   label: 'SHM Primary'
	// });

})(window.wp);
